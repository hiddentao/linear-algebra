language: node_js

node_js:
  - stable

branches:
  only:
    - master

before_script:
  - "npm install -g gulp"
  - "npm install"

script:
  - "npm run build"

notifications:
  email:
    - ram@hiddentao.com
